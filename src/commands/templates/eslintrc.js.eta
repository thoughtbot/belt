module.exports = {
  "extends": [
    "@thoughtbot/eslint-config/native",
    <% if(it.typescript) { %>
      "@thoughtbot/eslint-config/typescript"
    <% } %>
    ],
  <% if(it.typescript) { %>
  overrides: [
    {
      files: [
        '*.ts',
        '*.tsx',
        '*.d.ts'
      ],
      parserOptions: {
        project: './tsconfig.json'
      },
    },
  ],
  <% } %> 
};